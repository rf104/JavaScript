///While Loop

let index = 1;
while(index<=10){
    console.log(`Values of index is ${index}`);
    index++
}

let arr = ['Aref', 'Hafsa', 'Tawsif'];

let indx = 0;
while(indx<arr.length)
    {
        console.log(`Value of Index ${indx} is ${arr[indx]}`);
        indx++
    } 

    let score = 11;
    do{
        console.log(`Score is ${score}`); // in do-while at first make the change and then it check condition of while loop!!
    }while(score<=10)